from constants import TaskId
from meta.tasks import TaskTypeId

task_clear_memory = TaskId(1)
task_clear_memory_deep = TaskId(2)
task_visualize_movement = TaskId(3)

empire_task = TaskTypeId(1)
room_task = TaskTypeId(2)
